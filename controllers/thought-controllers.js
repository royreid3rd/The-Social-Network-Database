const { Thought, reactionSchema } = require('../models');

const thoughtController = {
  // Add controller methods here for GET, POST, PUT, DELETE operations for thoughts
};

module.exports = thoughtController;